apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: appns
data:
  CONNECTOR_URL: "http://connector-svc:8080"
  DJANGO_DEBUG: "False"
  DJANGO_ALLOWED_HOSTS: "*"
  DB_HOST: "postgres-svc"
  DB_PORT: "5432"
