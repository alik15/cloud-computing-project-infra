apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: gke-cicd-pipeline
description: Main delivery pipeline for Connector and Frontend apps
serialPipeline:
  stages:
  - targetId: dev
  - targetId: prod
